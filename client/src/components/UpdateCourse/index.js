import PropTypes from "prop-types";
import styles from "./index.module.css";

/**
 *
 * @returns {JSX.Element}
 */
export const UpdateCourse = () => <p>UpdateCourse</p>;

UpdateCourse.defaultProps = {};

UpdateCourse.propTypes = {};
