import PropTypes from "prop-types";
import styles from "./index.module.css";

/**
 *
 * @returns {JSX.Element}
 */
export const Courses = () => <p>Courses</p>;

Courses.defaultProps = {};

Courses.propTypes = {};
